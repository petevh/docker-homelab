http:
  routers:
    homeassistant:
      rule: "Host(`ha.app.vanheerden.ch`)"
      entrypoints:
        - websecure
      tls:
        certResolver: cloudflare
      service: homeassistant

  services:
    homeassistant:
      loadBalancer:
        servers:
          - url: "http://192.168.20.50:8123"
